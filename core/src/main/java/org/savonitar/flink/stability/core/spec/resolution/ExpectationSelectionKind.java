package org.savonitar.flink.stability.core.spec.resolution;

/** Whether an invocation selected the default contract or a parameter case. */
public enum ExpectationSelectionKind {
    DEFAULT,
    CASE
}
